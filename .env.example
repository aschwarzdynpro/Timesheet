# Supabase-Projekt: Einstellungen -> API
VITE_SUPABASE_URL=https://<projekt-ref>.supabase.co
# Der oeffentliche (publishable) Key. Niemals den service_role Key hier eintragen:
# er landet sonst im Browser-Bundle und haengt RLS komplett aus.
VITE_SUPABASE_ANON_KEY=<publishable-key>
